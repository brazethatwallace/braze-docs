# Verification: channel_performance Email Performance permissions

- Email Performance nav gate requires `viewAppUsage` (**View Usage Data**) OR `viewReporting` (**View Dashboard Reports**), or impersonation — `platform/dashboard/app/javascript/src/navigationRouting/dashboard/analytics_dashboard/__appGroupId/emailPerformanceNRO.tsx` (`getDeveloperNavigationAllowed`)
- UI label for `viewAppUsage`: **View Usage Data** — `platform/dashboard/app/javascript/src/lib/permissionConstants/permissionConstants.ts` / docs permissions table
- UI label for `viewReporting`: **View Dashboard Reports** — same permissionConstants file (`view-reporting-display-name`); feature flipper `clip_granular_permissions_all` may gate this permission in the UI
