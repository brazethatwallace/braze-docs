{% alert note %}
**日付フィールドについて:**
- `TIME` と `TIME_MS`: Brazeでユーザープロファイルの更新が発生した時刻を表します（それぞれ秒単位とミリ秒単位）。バックフィルされたデータの場合、これらの値はバックフィルが実行された時刻です。
- `SF_UPDATED_AT`: Snowflakeでデータが最後に永続化された時刻を表します。このフィールドは、データの鮮度を判断する際に最も役立ちます。つまり、データウェアハウスに行が最後に同期された時刻を示します。
{% endalert %}